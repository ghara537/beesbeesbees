var Bee = function() {
  // new Grub.call(this);
  // this.age = 5;
  // this.color = 'yellow';
  // this.job = 'keep on growing';
};

// Bee.prototype = Grub.prototype;
// Bee.prototype.constructor = Bee;


//Bee.prototype.job = 'keep on '